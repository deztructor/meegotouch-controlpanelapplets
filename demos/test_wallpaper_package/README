
This is provides two demo packages for the wallpaper applet new feature,
which allows the users to provide "wallpaper packs" trought the ovi-store.

Use this command to build the wallpaper1 and wallpaper2 packages:
dpkg-buildpackage -rfakeroot -b

PS.: Of course it is possible to include more than one background in one package.

